# OpenCode

修改 `~/.config/opencode/opencode.json` 文件(如果没有就新建)。

```json
{
    "$schema": "https://opencode.ai/config.json",
    "provider": {
        "openai": {
            "options": {
                "baseURL": "https://ai.cobitux.com/v1",
                "apiKey": "sk-dummy"
            }
        }
    },
    "model": "gpt-5.3-codex"
}
```