# Cursor

Cursor requires a paid subscription. We recommend using other tools instead.

1. Open Settings -> Models -> API Keys.
2. Set and enable `OpenAI API Key`.
3. Set `Override OpenAI Base URL` to `https://ai.cobitux.com/v1`.
4. Add any supported model to start using it. OpenAI spark models are not available.
